on:
  push:

jobs:
  first-job:
    name: "Test of First Job"
    runs-on: ubuntu-latest
    steps:
      - name: "uname"
        run: uname -a
      - name: "checkout sources"
        uses: actions/checkout@v3
      - uses: ./.github/workflows/system-info
      - name: "info"
        run: "pwd && ls -lA ; ls -lR .github"
